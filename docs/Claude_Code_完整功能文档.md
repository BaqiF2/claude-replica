# Claude Code 完整功能文档

> **版本说明**: 本文档基于 2025 年最新信息编写  
> **最新模型**: Claude Sonnet 4.5 (`claude-sonnet-4-5-20250929`) 和 Claude Haiku 4.5 (`claude-haiku-4-5-20251001`)

---

## 📋 目录

1. [产品概述](#产品概述)
2. [核心功能](#核心功能)
3. [安装与配置](#安装与配置)
4. [命令行接口 (CLI)](#命令行接口-cli)
5. [交互模式详解](#交互模式详解)
6. [扩展系统](#扩展系统)
7. [高级特性](#高级特性)
8. [最佳实践](#最佳实践)
9. [自动化与集成](#自动化与集成)
10. [企业级功能](#企业级功能)
11. [故障排除](#故障排除)

---

## 产品概述

### 什么是 Claude Code？

Claude Code 是 Anthropic 推出的**智能代码助手命令行工具**,它不仅仅是一个代码生成器,而是一个完整的 AI 驱动开发框架,能够:

- 🤖 **自主执行** - 直接编辑文件、运行命令、创建提交
- 🧠 **深度理解** - 维护整个项目结构的感知能力
- 🔧 **高度可扩展** - 通过插件、技能、MCP 服务器等方式扩展
- 🔄 **Unix 哲学** - 可组合、可脚本化、管道友好

### 产品定位

```
传统 IDE 辅助工具 → Claude Code → 完全自主的 AI 工程师
                    (你的位置)
```

Claude Code 定位在中间:它拥有足够的自主性来完成复杂任务,但仍然保持人类的监督和控制。

---

## 核心功能

### 1. 代码生成与编辑

#### 基本能力
- **从描述构建特性**: 用自然语言描述需求,Claude 制定计划、编写代码并确保正常工作
- **直接文件操作**: 无需等待权限即可编辑文件(某些 bash 命令除外)
- **多文件协调**: 跨多个文件进行一致性修改

#### 示例场景
```bash
# 创建完整的用户认证系统
claude "实现一个带有 JWT token 和密码哈希的用户认证系统"

# 重构现有代码
claude "重构这个 Java 类,使用更好的设计模式"
```

### 2. 调试与问题修复

#### 智能调试流程
1. **分析** - 解析错误信息和堆栈跟踪
2. **定位** - 在代码库中找到问题根源
3. **修复** - 实现解决方案
4. **验证** - 运行测试确认修复有效

#### 调试模式
```bash
# 粘贴错误信息
claude "这是我遇到的错误: [错误信息]"

# 分析日志文件
tail -f app.log | claude -p "如果看到异常,通过 Slack 通知我"
```

### 3. 代码库导航与理解

#### 全代码库感知
- 维护完整项目结构的意识
- 理解文件之间的依赖关系
- 快速定位相关代码片段

#### 使用方式
```bash
# 了解代码库
claude "分析这个代码库结构并提出改进建议。@./src/"

# 回答架构问题
claude "这个认证流程是如何工作的?"

# 查找特定功能
claude "用户注册的逻辑在哪里?"
```

### 4. Git 工作流自动化

#### Git 集成能力
- 📝 **自动生成提交信息** - 基于更改和历史记录
- 🔍 **搜索 Git 历史** - 回答"什么改变进入了 v1.2.3?"
- ⚔️ **处理复杂操作** - 还原文件、解决 rebase 冲突、比较和移植补丁

#### 实战示例
```bash
# 自动化提交流程
claude "review 我的更改,写一个好的提交信息,然后提交"

# 搜索历史
claude "查看 git 历史,告诉我这个 API 为什么这样设计"

# 解决冲突
claude "帮我解决这个 rebase 冲突"
```

### 5. 自动化繁琐任务

#### 常见自动化场景
- 🔧 修复 lint 问题
- 🔀 解决合并冲突  
- 📋 编写发布说明
- 🧪 生成样板代码
- 📊 更新文档

---

## 安装与配置

### 快速安装

#### 方式一: 使用 Claude Pro/Max 订阅
```bash
# 自动安装(推荐)
curl -fsSL https://anthropic.com/installer | sh

# 验证安装
claude --version
```

#### 方式二: 使用 API
```bash
# 设置 API 密钥
export ANTHROPIC_API_KEY='your-api-key-here'

# 安装
npm install -g @anthropic-ai/claude-code
# 或
pip install claude-code --break-system-packages
```

### 订阅计划选择

| 计划 | 适用场景 | Token 限制 | 价格 |
|------|---------|-----------|------|
| **Pro** | 中高强度编码工作 | 基础限额 | $20/月 |
| **Max5** | 密集编码工作 | Pro 的 5 倍 | $100/月 |
| **Max20** | 近乎自主的重度开发 | Pro 的 20 倍 | $200/月 |

> **💡 提示**: 会话从发送第一条消息开始,持续 5 小时。使用 Opus 会更快消耗 token。

### 目录结构

```
~/.claude/                    # 用户级配置
├── settings.json            # 用户设置
├── mcp.json                # MCP 服务器配置
├── agents/                 # 用户级子代理
├── commands/               # 用户级自定义命令
└── skills/                 # 用户级技能

<project>/.claude/           # 项目级配置
├── settings.json           # 项目设置(共享)
├── settings.local.json     # 本地设置(gitignored)
├── agents/                 # 项目子代理
├── commands/               # 项目自定义命令
└── skills/                 # 项目技能

<project>/.mcp.json          # 项目 MCP 服务器
<project>/CLAUDE.md          # 项目记忆/指令文档
```

---

## 命令行接口 (CLI)

### 基础命令

#### 启动交互会话
```bash
# 在当前目录启动
claude

# 在特定目录启动
cd /path/to/project && claude

# 带初始提示启动
claude "帮我实现一个新功能"
```

#### 非交互模式(Headless Mode)
```bash
# 打印模式 - 执行并退出
claude -p "分析这段代码的复杂度"

# 不同输出格式
claude -p "运行测试" --output-format json
claude -p "检查错误" --output-format stream-json

# 继续最近的会话
claude -c -p "继续刚才的工作"

# 恢复特定会话
claude --resume <session-id> -p "下一步是什么?"
```

### 核心标志 (Flags)

#### 系统提示定制
```bash
# 追加系统提示(推荐)
claude --append-system-prompt "你是一个安全专家"

# 完全替换系统提示
claude --system-prompt "自定义系统提示..."

# 从文件加载系统提示
claude --system-prompt-file ./custom-prompt.txt
```

#### 工具限制
```bash
# 限制可用工具
claude --allowedTools "Bash,Read,Grep"

# 限制到特定 MCP 工具
claude --allowedTools "mcp__github__create_issue"
```

#### 权限控制
```bash
# 跳过所有权限检查(危险!)
claude --dangerously-skip-permissions

# 计划模式(只读)
claude --permission-mode plan
```

#### 子代理动态注入
```bash
# 通过命令行添加子代理
claude --agents '{
  "code-reviewer": {
    "description": "专家代码审查员",
    "prompt": "你是资深代码审查员。关注质量、安全和最佳实践。",
    "tools": ["Read", "Grep", "Glob", "Bash"],
    "model": "sonnet"
  }
}'
```

### 管道与组合

Claude Code 遵循 Unix 哲学,可以与其他工具组合使用:

```bash
# 日志分析
tail -f app.log | claude -p "发现异常时通过 Slack 通知"

# Git diff 审查
git diff | claude -p "审查这些更改是否有安全问题"

# 批处理文件
find . -name "*.js" | xargs claude -p "为每个文件添加 JSDoc 注释"

# CI 集成
claude -p "如果有新的文本字符串,翻译成法语并为 @lang-fr-team 提交 PR"
```

---

## 交互模式详解

### 三种工作模式

#### 1. 自动模式 (Auto Mode) - 默认
- Claude 自动执行任务,无需每步确认
- 仍会请求某些 bash 命令的权限(如安装包)
- 按 `Esc` 可随时停止

```bash
claude
> 帮我构建一个 REST API
# Claude 自动开始工作...
```

#### 2. 计划模式 (Plan Mode)
- 使用扩展思考能力创建详细策略
- 适合复杂变更前的对齐
- 生成技术设计文档

```bash
claude --permission-mode plan
> 设计一个微服务架构
# Claude 制定详细计划而不执行
```

#### 3. 手动确认模式
```bash
# 通过 /permissions 配置需确认的命令
/permissions
> 添加: npm install -> 需要确认
```

### 交互式命令(Slash Commands)

#### 内置命令
```bash
/help              # 显示所有可用命令
/agents            # 管理子代理
/hooks             # 配置钩子
/mcp               # 管理 MCP 服务器
/permissions       # 查看/编辑权限
/config            # 打开设置
/plugin            # 管理插件
/bug               # 报告问题
```

#### 文件引用
```bash
# 引用文件或目录
> 分析 @src/main.js 的性能问题
> 重构 @./components/ 下的所有组件
```

#### 运行 Shell 命令
```bash
# 在对话中直接运行命令
> 请运行 `npm test` 并分析失败的测试
```

### 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Esc` | 停止当前操作 |
| `Esc + Esc` | 打开回退菜单(恢复之前状态)|
| `Cmd/Ctrl + V` | 粘贴(支持截图)|
| `Cmd/Ctrl + Shift + 4` (macOS) | 截图到剪贴板 |

---

## 扩展系统

Claude Code 拥有强大的扩展能力,通过五大扩展机制:

### 1. 技能 (Skills) ⭐

#### 什么是技能?
技能是**自动调用的模块化能力**,Claude 根据对话上下文自动加载。

#### 技能特点
- **自动调用** - 基于描述匹配,无需手动触发
- **上下文感知** - 根据任务需求智能加载
- **知识封装** - 包含领域专业知识和工作流程
- **可组合** - 多个技能可同时协作

#### 技能结构
```
skills/
└── my-skill/
    └── SKILL.md              # 技能定义文件
```

#### SKILL.md 示例
```markdown
---
name: java-expert
description: Java 和 Spring Boot 专家。当用户需要 Java 开发、Spring Boot 配置、JPA 使用或 Java 最佳实践时自动加载。
triggers:
  - Java development
  - Spring Boot
  - JPA configuration
tools:
  - Read
  - Bash
---

# Java 开发专家技能

## 核心能力
- Spring Boot 3.x 应用开发
- JPA/Hibernate 配置与优化
- 依赖注入最佳实践
- RESTful API 设计

## 编码标准
- 使用 Java 21 特性
- 遵循 Google Java Style Guide
- 所有 public 方法需要 JavaDoc
- 异常处理必须记录日志

## 常见任务
### 创建新的 REST 端点
1. 在 controller 包中创建 controller
2. 使用 @RestController 和 @RequestMapping
3. 实现请求处理方法
4. 添加异常处理
5. 编写单元测试
```

#### 技能使用场景

**场景一: 个人工作风格**
```markdown
---
name: personal-style
description: 霸气F2 的个人工作偏好和沟通风格。当为他起草邮件、Slack 消息或内部更新时自动加载。
---

## 沟通风格
- 使用中文进行技术讨论
- 喜欢详细的技术实现细节
- 偏好命令行工具而非图形界面

## 代码偏好
- 注重代码质量和可维护性
- 详细的注释和文档
- 使用 SLF4J 进行日志记录
```

**场景二: 技术栈专家**
```markdown
---
name: blockchain-expert
description: 区块链和 Web3j 开发专家。当处理以太坊智能合约、区块链数据或 Web3j API 时自动加载。
---

## 技术栈
- Web3j 4.x
- Ethereum JSON-RPC
- Solidity 智能合约

## 最佳实践
- 始终验证交易哈希
- 使用适当的 Gas 限制
- 实现重试机制处理 RPC 错误
```

### 2. 自定义命令 (Custom Commands)

#### 什么是自定义命令?
自定义命令是**手动触发的工作流模板**,通过 `/` 调用。

#### 命令结构
```
commands/
└── my-command.md             # 命令定义文件
```

#### 简单命令示例
```markdown
---
name: test
description: 运行所有单元测试并报告结果
---

请运行项目的单元测试套件:

1. 运行 `npm test` 或 `mvn test`
2. 分析测试结果
3. 如果有失败,展示失败的测试和原因
4. 提供修复建议
```

#### 带参数的命令
```markdown
---
name: fix-github-issue
argument-hint: [issue-number]
description: 分析并修复 GitHub issue
---

请分析并修复 GitHub issue: $ARGUMENTS

步骤:
1. 使用 `gh issue view $ARGUMENTS` 获取 issue 详情
2. 理解问题描述
3. 在代码库中搜索相关文件
4. 实施必要的更改
5. 编写并运行测试验证修复
6. 确保代码通过 lint 和类型检查
7. 创建描述性的提交信息
8. 推送并创建 PR
```

#### 使用自定义命令
```bash
# 列出所有命令
/help

# 使用项目命令
/project:test

# 使用带参数的命令
/project:fix-github-issue 1234

# 使用用户级命令
/user:deploy-staging
```

#### 高级命令特性

**工具限制**
```markdown
---
name: safe-commit
allowed-tools: 
  - Bash(git add:*)
  - Bash(git status:*)
  - Bash(git commit:*)
argument-hint: [message]
---

## 上下文
- 当前 git 状态: !`git status`
- 当前 git diff: !`git diff HEAD`
- 当前分支: !`git branch --show-current`

创建 git 提交: $ARGUMENTS
```

**包含文件内容**
```markdown
---
name: review-changes
---

请审查以下更改:

当前更改:
!`git diff HEAD`

最近的提交:
!`git log --oneline -5`
```

### 3. 子代理 (Subagents)

#### 什么是子代理?
子代理是**专门化的 Claude 实例**,拥有独立的上下文窗口和角色定位。

#### 子代理架构
```
你 ↔ 主 Claude(协调者)
     ├── 代码审查员(质量专家)
     ├── 测试工程师(测试专家)
     └── 文档编写员(技术写作专家)
```

#### 创建子代理
```bash
# 使用交互式命令创建
/agents
> 创建一个代码审查子代理

# 或通过命令行
claude --agents '{
  "reviewer": {
    "description": "用于深入代码审查",
    "model": "sonnet",
    "color": "orange",
    "prompt": "你是专家代码审查员。关注安全、性能和可维护性。"
  }
}'
```

#### 子代理配置文件
```markdown
---
name: reviewer
description: 用于深入代码审查
model: sonnet
color: orange
---

你是专家代码审查员。关注:

## 审查重点
- 安全漏洞
- 性能瓶颈
- 代码可维护性
- 遵循最佳实践

## 审查流程
1. 全面阅读代码变更
2. 识别潜在问题
3. 提供具体改进建议
4. 评估测试覆盖率
```

#### 使用子代理
```bash
# 显式调用
> @reviewer 请审查这个 PR

# 自动路由(基于描述匹配)
> 请审查我的代码更改
# Claude 自动将任务路由给 reviewer
```

#### 子代理优势
- ✅ **隔离上下文** - 不会污染主对话的上下文窗口
- ✅ **专业化** - 每个代理专注特定领域
- ✅ **并行处理** - 可以同时处理多个任务
- ✅ **工具限制** - 可以限制特定代理的工具访问

### 4. 钩子 (Hooks)

#### 什么是钩子?
钩子是**事件驱动的自动化机制**,在特定工具使用后自动触发。

#### 钩子类型
```json
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "npm run lint:fix $FILE"
          }
        ]
      }
    ]
  }
}
```

#### 常见钩子场景

**自动 Lint**
```json
{
  "PostToolUse": [{
    "matcher": "Write",
    "hooks": [{
      "type": "command",
      "command": "eslint --fix $FILE"
    }]
  }]
}
```

**自动格式化**
```json
{
  "PostToolUse": [{
    "matcher": "Write.*\\.java$",
    "hooks": [{
      "type": "command",
      "command": "google-java-format -i $FILE"
    }]
  }]
}
```

**自动测试**
```json
{
  "PostToolUse": [{
    "matcher": "Write.*test\\.ts$",
    "hooks": [{
      "type": "command",
      "command": "npm test $FILE"
    }]
  }]
}
```

#### 配置钩子
```bash
# 交互式配置
/hooks

# 或编辑配置文件
# .claude/settings.json 或 settings.local.json
```

### 5. 插件 (Plugins)

#### 什么是插件?
插件是**打包的扩展集合**,可以包含命令、代理、技能、钩子和 MCP 服务器。

#### 插件结构
```
my-plugin/
├── .claude-plugin/
│   └── plugin.json          # 插件清单
├── commands/                # 自定义命令
│   └── my-command.md
├── agents/                  # 子代理
│   └── my-agent.md
├── skills/                  # 技能
│   └── my-skill/
│       └── SKILL.md
├── hooks/
│   └── hooks.json          # 钩子配置
└── .mcp.json               # MCP 服务器配置
```

#### plugin.json 示例
```json
{
  "name": "java-toolkit",
  "version": "1.0.0",
  "description": "Java 开发工具包",
  "author": "霸气F2",
  "repository": "https://github.com/user/java-toolkit",
  "dependencies": {
    "java": ">=21",
    "maven": ">=3.8"
  }
}
```

#### 安装插件
```bash
# 从市场安装
claude plugin install java-toolkit

# 从本地安装
claude --plugin-dir /path/to/my-plugin

# 从 Git 仓库安装
claude plugin install github:user/repo
```

#### 创建插件
```bash
# 创建插件目录结构
mkdir -p my-plugin/.claude-plugin
mkdir -p my-plugin/{commands,agents,skills,hooks}

# 创建插件清单
cat > my-plugin/.claude-plugin/plugin.json << 'EOF'
{
  "name": "my-plugin",
  "version": "1.0.0",
  "description": "我的自定义插件"
}
EOF
```

### 6. MCP 服务器集成

#### 什么是 MCP?
Model Context Protocol (MCP) 是**标准化的 AI 工具集成协议**,类似于 AI 的 USB-C。

#### MCP vs 其他扩展

| 特性 | Skills | Commands | MCP |
|------|--------|----------|-----|
| 触发方式 | 自动 | 手动 | 工具调用 |
| 用途 | 知识/工作流 | 工作流模板 | 外部系统连接 |
| 示例 | 编码标准 | 测试流程 | GitHub API |

#### MCP 配置方式

**1. 项目级配置 (.mcp.json)**
```json
{
  "filesystem": {
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-filesystem", "/path/to/allowed/dir"]
  }
}
```

**2. 用户级配置 (~/.claude/mcp.json)**
```json
{
  "slack": {
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-slack"],
    "env": {
      "SLACK_BOT_TOKEN": "${SLACK_BOT_TOKEN}",
      "SLACK_TEAM_ID": "${SLACK_TEAM_ID}"
    }
  }
}
```

**3. 命令行添加**
```bash
# 添加 stdio 传输 MCP
claude mcp add filesystem \
  -- npx -y @modelcontextprotocol/server-filesystem /path/to/allowed/dir

# 添加 SSE 传输 MCP
claude mcp add --transport sse myservice https://api.example.com/mcp

# 添加 HTTP 传输 MCP
claude mcp add --transport http myapi https://api.example.com/mcp
```

#### 常用 MCP 服务器

**开发工具**
```bash
# Filesystem
claude mcp add filesystem -- npx -y @modelcontextprotocol/server-filesystem /path/to/allowed/dir

# GitLab
claude mcp add gitlab --env GITLAB_TOKEN=$TOKEN -- npx -y @modelcontextprotocol/server-gitlab

# Jira
claude mcp add jira --env JIRA_TOKEN=$TOKEN -- npx -y @modelcontextprotocol/server-jira
```

**数据库**
```bash
# PostgreSQL
claude mcp add postgres --env DATABASE_URL=$URL -- npx -y @modelcontextprotocol/server-postgres

# MongoDB
claude mcp add mongodb --env MONGODB_URI=$URI -- npx -y @modelcontextprotocol/server-mongodb
```

**生产力工具**
```bash
# Slack
claude mcp add slack --env SLACK_BOT_TOKEN=$TOKEN -- npx -y @modelcontextprotocol/server-slack

# Google Drive
claude mcp add gdrive --env GOOGLE_CREDENTIALS=$CREDS -- npx -y @modelcontextprotocol/server-gdrive

# Notion
claude mcp add notion --env NOTION_TOKEN=$TOKEN -- npx -y @modelcontextprotocol/server-notion
```

#### MCP 工具命名
MCP 工具在 Claude Code 中使用特殊前缀:
```
mcp__<server-name>__<tool-name>
mcp__plugin_<plugin-name>_<server-name>__<tool-name>
```

示例:
```markdown
---
allowed-tools:
  - mcp__github__create_issue
  - mcp__github__search_code
  - mcp__plugin_asana_asana__asana_create_task
---
```

#### 调试 MCP
```bash
# 启用 MCP 调试模式
claude --mcp-debug

# 查看 MCP 服务器状态
/mcp
```

---

## 高级特性

### 1. CLAUDE.md - 项目记忆

#### 什么是 CLAUDE.md?
CLAUDE.md 是项目的**持久性上下文文档**,类似于项目宪法。

#### CLAUDE.md 内容建议

```markdown
# 项目上下文

## 项目信息
- 名称: 企业 CRM 系统
- 技术栈: Spring Boot 3.3 + Java 21 + PostgreSQL
- 负责人: 霸气F2

## 编码标准
- 使用 Java 21 特性(记录类、模式匹配)
- 遵循 Google Java Style Guide
- 所有 public API 需要 JavaDoc
- 使用 SLF4J 进行日志记录

## 架构
- 前端: React + TypeScript
- 后端: Spring Boot REST API
- 数据库: PostgreSQL with Prisma
- 缓存: Redis

## 文件组织
- 控制器: `src/main/java/com/example/controller/`
- 服务: `src/main/java/com/example/service/`
- 仓储: `src/main/java/com/example/repository/`
- 测试: `src/test/java/` (镜像主代码结构)

## 工作流程
1. 所有功能必须先写测试
2. 代码变更需要通过 CI/CD 管道
3. PR 需要至少一人审查
4. 使用语义化版本控制

## 常见任务
### 添加新的 REST 端点
1. 在 controller 包创建新 controller
2. 实现服务层逻辑
3. 添加 JPA 仓储(如需要)
4. 编写单元测试和集成测试
5. 更新 API 文档

## 禁止事项
- ❌ 不要使用 `System.out.println` - 使用日志框架
- ❌ 不要直接在 controller 中写业务逻辑
- ❌ 不要提交敏感信息到 Git
```

### 2. 图像和图表支持

#### 支持的使用场景
- 📊 **设计模拟** - 上传 UI 设计图,让 Claude 实现
- 📈 **图表分析** - 分析可视化数据
- 🐛 **截图调试** - 展示 UI 错误
- 📐 **架构图** - 上传系统架构图进行讨论

#### 使用方式

**方式一: 粘贴(macOS)**
```bash
# 1. 截图到剪贴板
Cmd + Ctrl + Shift + 4

# 2. 在 Claude 中粘贴
Ctrl + V  # 注意:不是 Cmd + V
```

**方式二: 拖放**
```bash
# 直接拖动图片到 Claude 提示输入区
```

**方式三: 文件路径**
```bash
> 根据 @./mockup.png 实现这个 UI
```

#### 设计实现工作流
```bash
# 1. 上传设计图
> 这是我想要的 UI 设计 [粘贴截图]

# 2. Claude 实现代码
> 请用 React 和 Tailwind CSS 实现这个设计

# 3. 迭代优化
> 颜色应该更柔和一些,增加圆角

# 4. 截图对比
> 看起来很好!请提交代码
```

### 3. 回退系统 (Rewind)

#### 什么是回退?
Claude Code 自动跟踪所有文件编辑,允许快速恢复到之前的状态。

#### 使用回退
```bash
# 打开回退菜单
Esc + Esc

# 菜单会显示:
# - 最近的文件更改
# - 时间戳
# - 变更摘要
```

#### 回退场景
- ❌ Claude 的更改出错
- 🔄 想尝试不同方法
- 🎯 回到已知正常的状态
- 📊 比较不同实现方案

### 4. 会话管理

#### 会话特性
- ⏰ **5 小时有效期** - 从第一条消息开始
- 💾 **自动保存** - 完整消息历史和工具状态
- 🔄 **可恢复** - 随时继续之前的会话

#### 会话命令
```bash
# 列出所有会话
claude sessions

# 恢复特定会话
claude --resume <session-id>

# 继续最近会话(非交互)
claude -c -p "继续刚才的工作"

# 删除旧会话
claude sessions clean --older-than 7d
```

#### 多会话策略
```bash
# 为不同任务创建独立会话
claude  # 会话 1: 功能开发
claude  # 会话 2: Bug 修复
claude  # 会话 3: 代码审查
```

### 5. 浏览器集成(Chrome 扩展)

#### Chrome 扩展能力
- 🌐 **浏览器自动化** - 控制浏览器进行测试
- 📸 **网页截图** - 自动获取页面截图
- 🔍 **元素检查** - 分析 DOM 结构
- ✅ **E2E 测试** - 端到端自动化测试

#### 使用场景
```bash
# UI 实现和验证
> 访问 http://localhost:3000,截图,然后告诉我和设计图的差异

# 网页数据提取
> 访问这个竞品网站,分析他们的定价策略

# 自动化测试
> 测试登录流程:访问首页,填写表单,提交,验证跳转
```

---

## 最佳实践

### 1. 提示工程最佳实践

#### 明确具体
```bash
# ❌ 不好
"优化这段代码"

# ✅ 好
"重构这个 Java 类,使用策略模式替换 if-else 链,并添加单元测试"
```

#### 提供上下文
```bash
# ✅ 包含相关文件
"重构 @src/UserService.java,使其符合 @CLAUDE.md 中的编码标准"

# ✅ 引用错误信息
"修复这个错误: [完整堆栈跟踪]"

# ✅ 描述预期行为
"用户点击提交按钮后,应该看到成功消息,然后重定向到仪表板"
```

#### 分步骤处理复杂任务
```bash
# 步骤 1: 研究
"分析当前的认证系统,识别安全漏洞"

# 步骤 2: 计划
"制定添加 2FA 的实现计划"

# 步骤 3: 实现
"按照计划实现 2FA 功能"

# 步骤 4: 验证
"编写集成测试验证 2FA 工作正常"

# 步骤 5: 文档
"更新文档说明如何使用 2FA"
```

### 2. 测试驱动开发(TDD)工作流

#### TDD 循环
```bash
# 1. 写失败的测试
"为用户注册功能编写测试(还不需要实现)"

# 2. 实现最小代码
"实现代码让测试通过"

# 3. 重构
"重构实现代码,提高可读性"

# 4. 验证
"运行所有测试确保没有破坏现有功能"
```

#### 自动化 TDD
```markdown
---
name: tdd-cycle
---

# TDD 开发循环

对于功能 $ARGUMENTS:

1. **Red**: 编写失败的测试
   - 运行: `npm test` 确认失败
   
2. **Green**: 编写最小实现
   - 让测试通过
   - 再次运行测试确认

3. **Refactor**: 改进代码
   - 提高可读性
   - 消除重复
   - 保持测试通过

4. **Verify**: 最终检查
   - 运行完整测试套件
   - 检查代码覆盖率
   - 运行 linter
```

### 3. Git 最佳实践

#### 原子提交工作流
```bash
# 1. 实现小的、独立的更改
"添加用户邮箱验证,不要包含其他更改"

# 2. 审查更改
"检查 git diff,确保只有相关更改"

# 3. 创建描述性提交
"写一个好的提交信息并提交"

# 4. 可选:创建 PR
"创建 PR,标题是 '添加邮箱验证',正文包含更改说明"
```

#### 分支策略
```bash
# 功能分支
"创建新分支 feature/email-verification 并切换"

# 完成后
"合并到 main 并删除功能分支"

# 冲突解决
"拉取最新的 main,解决任何合并冲突"
```

### 4. 代码审查工作流

#### 使用子代理审查
```bash
# 1. 完成开发
"实现用户认证功能"

# 2. 自我审查
"审查我的更改,查找明显问题"

# 3. 子代理深度审查
"@reviewer 请深度审查这些更改,关注安全性和性能"

# 4. 应用反馈
"应用审查建议并更新代码"

# 5. 最终检查
"@reviewer 快速检查更新后的代码"
```

#### 审查清单
```markdown
# 代码审查清单

## 功能性
- [ ] 代码实现了需求的功能
- [ ] 边界情况得到处理
- [ ] 错误处理适当

## 代码质量
- [ ] 代码可读且有适当注释
- [ ] 遵循项目编码标准
- [ ] 没有明显的代码异味

## 测试
- [ ] 有足够的测试覆盖
- [ ] 测试有意义且通过
- [ ] 包含边界情况测试

## 安全性
- [ ] 没有明显的安全漏洞
- [ ] 敏感数据得到保护
- [ ] 输入得到验证

## 性能
- [ ] 没有明显的性能问题
- [ ] 资源使用合理
- [ ] 数据库查询优化
```

### 5. 项目入职

#### Anthropic 的入职实践
```bash
# 新工程师入职流程
claude

> 我刚加入团队,请帮我理解这个代码库
> 
> 1. 高层架构是什么?
> 2. 主要组件有哪些?
> 3. 数据如何流动?
> 4. 如何设置开发环境?
> 5. 第一个 PR 应该从哪里开始?
```

#### 创建入职技能
```markdown
---
name: onboarding
description: 帮助新团队成员理解项目。当询问项目概述、架构或入门指南时加载。
---

# 项目入职指南

## 架构概述
[项目的高层架构描述]

## 关键概念
[核心概念和术语表]

## 开发环境设置
1. 克隆仓库
2. 安装依赖
3. 配置环境变量
4. 运行本地服务器

## 第一个任务建议
- 修复标记为 "good first issue" 的问题
- 添加单元测试提高覆盖率
- 改进文档

## 关键联系人
- 架构: @architect
- 前端: @frontend-lead
- 后端: @backend-lead
```

### 6. 调试策略

#### 系统化调试
```bash
# 1. 重现问题
"我遇到这个错误: [错误信息]。环境是 [描述]"

# 2. 收集信息
"检查日志文件 @logs/app.log 的最后 100 行"

# 3. 形成假设
"基于错误和日志,你认为问题可能是什么?"

# 4. 测试假设
"添加调试日志来验证假设"

# 5. 实施修复
"实现修复并验证问题解决"

# 6. 防止复发
"添加测试确保这个 bug 不会再次出现"
```

#### 日志分析
```bash
# 实时监控
tail -f app.log | claude -p "监控日志中的错误模式,发现异常时提醒我"

# 历史分析
cat app.log | claude -p "分析这个日志文件,找出导致服务中断的原因"
```

---

## 自动化与集成

### 1. CI/CD 集成

#### GitHub Actions 集成

**自动代码审查**
```yaml
# .github/workflows/claude-review.yml
name: Claude Code Review

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Review with Claude
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          npm install -g @anthropic-ai/claude-code
          
          # 获取 PR diff
          gh pr diff ${{ github.event.pull_request.number }} > changes.diff
          
          # Claude 审查
          claude -p --output-format json "审查这个 PR 的安全性和性能问题" \
            < changes.diff > review.json
          
          # 发布评论
          gh pr comment ${{ github.event.pull_request.number }} \
            --body-file review.json
```

**自动修复 Issues**
```yaml
# .github/workflows/claude-fix-issue.yml
name: Auto-fix Issues

on:
  issues:
    types: [labeled]

jobs:
  fix:
    if: github.event.label.name == 'auto-fix'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Fix Issue
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          # 获取 issue 详情
          ISSUE_BODY=$(gh issue view ${{ github.event.issue.number }} --json body -q .body)
          
          # Claude 修复
          claude -p --dangerously-skip-permissions \
            "分析并修复 issue #${{ github.event.issue.number }}: $ISSUE_BODY"
          
          # 创建 PR
          gh pr create \
            --title "Fix: #${{ github.event.issue.number }}" \
            --body "自动修复 issue #${{ github.event.issue.number }}"
```

**自动测试生成**
```yaml
# .github/workflows/claude-tests.yml
name: Generate Tests

on:
  push:
    paths:
      - 'src/**/*.ts'
      - '!src/**/*.test.ts'

jobs:
  generate-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Generate Missing Tests
        run: |
          # 找到没有测试的文件
          for file in $(find src -name "*.ts" ! -name "*.test.ts"); do
            test_file="${file%.ts}.test.ts"
            if [ ! -f "$test_file" ]; then
              claude -p "为 $file 生成全面的单元测试"
            fi
          done
          
          # 提交新测试
          git add .
          git commit -m "chore: add missing tests"
          git push
```

### 2. Pre-commit Hooks

#### 自动 Lint 修复
```bash
# .git/hooks/pre-commit
#!/bin/bash

# 获取暂存的文件
STAGED_FILES=$(git diff --cached --name-only --diff-filter=ACM)

# 让 Claude 修复 lint 问题
echo "$STAGED_FILES" | claude -p "修复这些文件的 lint 错误,不要更改功能"

# 重新暂存修复后的文件
git add $STAGED_FILES
```

#### 提交信息生成
```bash
# .git/hooks/prepare-commit-msg
#!/bin/bash

COMMIT_MSG_FILE=$1

# 如果提交信息为空,让 Claude 生成
if [ ! -s "$COMMIT_MSG_FILE" ]; then
  git diff --cached | claude -p --output-format stream-json \
    "基于这些更改生成符合约定式提交的提交信息" \
    > "$COMMIT_MSG_FILE"
fi
```

### 3. 定时任务

#### 依赖更新
```bash
# crontab: 每周一检查依赖更新
0 9 * * 1 cd /project && claude -p "检查过时的依赖并创建更新 PR"
```

#### 性能监控
```bash
# 每天分析性能日志
0 2 * * * cd /project && cat performance.log | claude -p "分析性能趋势,报告降级"
```

#### 安全审计
```bash
# 每月安全审计
0 0 1 * * cd /project && claude -p "进行安全审计,识别潜在漏洞"
```

### 4. 批处理操作

#### 批量文件处理
```bash
# 为所有组件添加 PropTypes
find src/components -name "*.jsx" | while read file; do
  claude -p "为 $file 添加 PropTypes 验证"
done

# 批量重构
find . -name "*.java" | claude -p --dangerously-skip-permissions \
  "将所有这些文件从 Java 8 升级到 Java 21 语法"
```

#### 批量测试生成
```bash
# 生成缺失的测试
for file in src/**/*.ts; do
  if [ ! -f "${file%.ts}.test.ts" ]; then
    claude -p "为 $file 生成测试文件 ${file%.ts}.test.ts"
  fi
done
```

---

## 企业级功能

### 1. API 访问

#### 配置 API
```bash
# 设置 API 密钥
export ANTHROPIC_API_KEY='sk-ant-...'

# 自定义 API 端点(企业部署)
export ANTHROPIC_API_URL='https://api.enterprise.com'

# 验证配置
claude -p "test" --output-format json
```

#### 按需付费 vs 订阅

| 使用模式 | 推荐方案 | 考虑因素 |
|---------|---------|---------|
| 偶尔使用 | 按需付费 API | 成本效益 |
| 频繁使用 | Pro/Max 订阅 | Token 刷新窗口 |
| 团队部署 | 企业 API | 统一管理 |

### 2. Docker Sandbox

#### 配置自定义 API
```json
{
  "dockerSandbox": {
    "apiEndpoint": "https://custom-api.company.com",
    "apiKey": "${CUSTOM_API_KEY}"
  }
}
```

### 3. 团队协作

#### 共享配置
```bash
# 项目级配置(检入 Git)
.claude/
├── settings.json          # 团队共享设置
├── agents/               # 团队子代理
├── commands/             # 团队命令
└── skills/               # 团队技能

# 本地配置(不检入)
.claude/
└── settings.local.json   # 个人覆盖设置
```

#### 配置优先级
```
settings.local.json > .claude/settings.json > ~/.claude/settings.json
```

### 4. 审计与合规

#### 完整审计日志
```bash
# GitHub Actions 自动记录所有 Claude 操作
- name: Run with Audit
  run: |
    claude -p "${{ inputs.task }}" \
      --output-format stream-json \
      | tee -a audit.log
    
    # 上传审计日志
    aws s3 cp audit.log s3://audit-bucket/$(date +%Y%m%d)/
```

#### 会话回顾
```bash
# 定期审查 Claude 会话
claude sessions | claude -p "分析常见错误模式,建议改进 CLAUDE.md"
```

---

## 故障排除

### 常见问题

#### 1. 安装问题

**问题**: `command not found: claude`
```bash
# 解决方案 1: 检查 PATH
echo $PATH | grep claude

# 解决方案 2: 重新安装
curl -fsSL https://anthropic.com/installer | sh
source ~/.bashrc

# 解决方案 3: 手动添加到 PATH
export PATH="$HOME/.claude/bin:$PATH"
```

**问题**: Python 包安装失败
```bash
# 在 Claude Code 的 bash 中始终使用 --break-system-packages
pip install pandas --break-system-packages
```

#### 2. 权限问题

**问题**: Claude 不断请求权限
```bash
# 解决方案 1: 配置权限
/permissions
> 添加白名单: npm install, git commit

# 解决方案 2: 危险模式(谨慎使用)
claude --dangerously-skip-permissions

# 解决方案 3: 计划模式(只读)
claude --permission-mode plan
```

#### 3. MCP 连接问题

**问题**: MCP 服务器无法连接
```bash
# 1. 启用调试
claude --mcp-debug

# 2. 检查 MCP 配置
/mcp

# 3. 验证环境变量（如需要）
echo $DATABASE_URL

# 4. 测试 MCP 服务器独立运行
npx -y @modelcontextprotocol/server-postgres
```

#### 4. 性能问题

**问题**: Claude 响应缓慢
```bash
# 1. 检查网络
ping api.anthropic.com

# 2. 使用更快的模型
claude --model haiku  # 更快但能力稍弱

# 3. 减少上下文
# 不要包含不必要的大文件

# 4. 使用子代理
# 隔离不同任务的上下文
```

**问题**: Token 消耗过快
```bash
# 1. 使用子代理
# 防止上下文窗口膨胀

# 2. 创建新会话
# 为不同任务使用独立会话

# 3. 优化 CLAUDE.md
# 只包含必要信息

# 4. 使用技能而非重复提示
# 将常用指令封装为技能
```

#### 5. 技能和插件问题

**问题**: 技能没有被加载
```bash
# 1. 检查技能位置
ls -la .claude/skills/
ls -la ~/.claude/skills/

# 2. 验证 SKILL.md 格式
# 确保有正确的 YAML frontmatter

# 3. 检查描述匹配
# 描述应该清晰表明何时使用技能

# 4. 重启 Claude Code
# 技能在启动时加载
```

**问题**: 插件安装失败
```bash
# 1. 检查插件清单
cat plugin/.claude-plugin/plugin.json

# 2. 验证依赖
# 确保所有依赖已安装

# 3. 使用 --plugin-dir 测试
claude --plugin-dir ./my-plugin

# 4. 检查日志
claude --verbose
```

### 调试技巧

#### 使用 Verbose 模式
```bash
# 启用详细输出
claude --verbose

# 查看完整的工具调用
export CLAUDE_DEBUG=1
claude
```

#### 检查配置
```bash
# 查看有效配置
/config

# 检查 MCP 服务器
/mcp

# 检查权限
/permissions

# 列出可用代理
/agents
```

#### 日志分析
```bash
# Claude Code 日志位置
~/.claude/logs/

# 查看最近的日志
tail -f ~/.claude/logs/claude-code.log

# 搜索错误
grep -i error ~/.claude/logs/claude-code.log
```

---

## 资源链接

### 官方文档
- 📚 [Claude Code 文档](https://code.claude.com/docs)
- 📚 [Claude API 文档](https://docs.anthropic.com)
- 💬 [Discord 社区](https://discord.gg/claude-developers)
- 🐙 [GitHub 仓库](https://github.com/anthropics/claude-code)

### 市场和资源
- 🛍️ [插件市场](https://claude-plugins.dev)
- 🎯 [技能库](https://mcpservers.org/claude-skills)
- 🔧 [MCP 服务器目录](https://github.com/modelcontextprotocol/servers)
- ✨ [Awesome Claude Code](https://github.com/hesreallyhim/awesome-claude-code)

### 学习资源
- 📝 [最佳实践指南](https://www.anthropic.com/engineering/claude-code-best-practices)
- 📖 [完整教程](https://www.siddharthbharath.com/claude-code-the-complete-guide/)
- 🎥 [视频教程合集](https://www.youtube.com/c/anthropic)

---

## 附录

### A. 快速参考卡

#### 基本命令
```bash
claude                    # 启动交互会话
claude -p "query"        # 非交互查询
claude -c                # 继续最近会话
claude --help            # 帮助信息
```

#### 交互命令
```bash
/help         # 帮助
/agents       # 子代理管理
/mcp          # MCP 管理
/permissions  # 权限管理
/config       # 设置
/plugin       # 插件管理
```

#### 文件操作
```bash
@file.txt           # 引用文件
@./dir/             # 引用目录
!`command`          # 嵌入命令输出
$ARGUMENTS          # 命令参数
```

### B. 配置模板

#### settings.json 模板
```json
{
  "model": "sonnet",
  "maxTokens": 8000,
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "npm run lint:fix $FILE"
          }
        ]
      }
    ]
  },
  "defaultTools": [
    "Read",
    "Write",
    "Bash",
    "Grep"
  ]
}
```

#### .mcp.json 模板
```json
{
  "filesystem": {
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-filesystem", "/path/to/allowed/dir"]
  }
}
```

### C. 术语表

- **Skill (技能)**: 自动加载的领域知识或工作流指南
- **Command (命令)**: 手动触发的工作流模板
- **Subagent (子代理)**: 具有独立上下文的专用 Claude 实例
- **Hook (钩子)**: 工具使用后自动触发的操作
- **Plugin (插件)**: 打包的扩展集合
- **MCP**: Model Context Protocol,外部工具集成协议
- **Headless Mode**: 非交互模式,用于脚本和自动化
- **Session (会话)**: 有 5 小时有效期的对话实例

---

## 结语

Claude Code 不仅仅是一个 AI 编程助手,它代表了软件开发的新范式。通过掌握本文档中的功能和最佳实践,你可以:

✅ **10 倍提升生产力** - 自动化重复任务,专注创造性工作  
✅ **提高代码质量** - 一致的标准、全面的测试、深入的审查  
✅ **加速学习** - 快速理解新代码库,有效的团队入职  
✅ **减少认知负担** - 让 AI 处理繁琐细节,你关注核心逻辑  

记住:Claude Code 的强大来自于**人机协作**。它不是要取代开发者,而是要增强你的能力,让你成为更好的工程师。

祝编码愉快!🚀

---

*文档版本: 1.0*  
*最后更新: 2025-01-01*  
*作者: 基于 Anthropic 官方文档和社区最佳实践整理*
